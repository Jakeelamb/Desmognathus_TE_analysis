# TODO

This file is the live queue only. Completed audit history belongs in the
generated result tables and focused audit notes, not in a growing task archive.

## Current State

- Status: corrected final-18 TE, ectopic-proxy, tree, microscopy, and
  exploratory path audits are frozen under `results/data/corrected/`.
- The primary executable collaborator bundle is `notebooks/research_review/`
  (eight domain notebooks plus a fail-closed integrity manifest). The combined
  notebook remains a secondary cross-domain workbench.
- Absolute image-derived genome size, segmentation generalization, and causal
  path claims remain blocked; see the machine-readable release-gate matrix.
- Current source traceability gaps are generated by
  `path_analysis/scripts/audit_source_traceability.py`.

## Current Integration Queue

1. Review the eight corrected research-review notebooks and weird-result queue with
   collaborators; record decisions without overwriting the frozen audit branch.
2. Obtain the focal tree citation/calibration record and missing LTR mapping
   provenance; keep the terminal:internal metric proxy-labeled.
3. Finish blinded cell/nucleus review and build specimen/slide-held-out truth
   containing final-panel species and difficult-object strata.
4. Recover a DNA-stoichiometric same-batch reference calibration and external
   validation for genome size, or remove genome-size wording from the study.
5. After new measurement evidence exists, freeze a prospective DAG set and fit
   an error-aware phylogenetic SEM with specimen and TE uncertainty.

## Working Rules

- Every new analysis must write a machine-readable result table under
  `results/data/` or `path_analysis/data/derived/`.
- Every new analysis must record explicit upstream source paths.
- Prefer analyses that strengthen trust in current TE/genome interpretation over
  broad exploratory side projects.
- Keep primary claims tied to observed/source-backed data.
- Keep sensitivity layers clearly labeled as sensitivity layers.

## Stop Conditions

- Stop adding TE-derived predictors if trust checks show strong assembly or
  method artifacts.
- Stop adding path families if the current families fail basic stability checks.
- Do not promote historical TE variables unless they survive the same source,
  trust, and stability checks as the primary TE features.
